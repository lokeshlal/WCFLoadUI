<Window x:Class="WCFLoadUI.Views.ServiceUrlView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="120" Width="700" 
        ResizeMode="NoResize" 
        ShowInTaskbar="False" 
        WindowStartupLocation="CenterScreen">
    <Grid FocusManager.FocusedElement="{Binding ElementName=ServiceUrl}">
        <DockPanel>
            <TextBox x:Name="ServiceUrl" 
                 Height="40" 
                 FontSize="18" 
                    Margin="10,16,0,16" 
                     Width="612" 
                     ClipToBounds="True" 
                     SnapsToDevicePixels="True" 
                     ToolTip="Enter Service Url"
                     Text="{Binding ServiceUrl}"
                     Focusable="True" />
            <Button x:Name="AddServiceUrl" Width="50" Height="50"
                    HorizontalAlignment="Right" DockPanel.Dock="Right" Margin="0,0,10,0">Add</Button>
        </DockPanel>
        
    </Grid>
</Window>
